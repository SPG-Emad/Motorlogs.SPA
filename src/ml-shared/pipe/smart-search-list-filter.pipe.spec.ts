/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SmartSearchListFilterPipe } from './smart-search-list-filter.pipe';

describe('Pipe: SmartSearchListFiltere', () => {
  it('create an instance', () => {
    let pipe = new SmartSearchListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
